apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: poc-kuma
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: jaimehenao8126/frontend:latest
          ports:
            - containerPort: 80